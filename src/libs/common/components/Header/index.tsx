import styles from './Header.module.scss'

/**
 * @description
 * 공용으로 쓰일 기본 헤더
 */
export default function Header() {
  return (
    <header className={styles.header}>
      TODO: 헤더로 라우트 이동 간단하게 하기
    </header>
  )
}
